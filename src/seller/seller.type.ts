export type CreateAccountSchema = {
  stripeAccountId: string;
};

export type CreateAccountSessionSchema = {
  client_secret: string;
};

export type ViewSellerStripeAccount = {
  stripeAccountId: string;
};
